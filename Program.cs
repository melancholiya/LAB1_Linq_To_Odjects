using LAB1;
using System;
using System.Collections.Generic;
using System.Linq;


class Program
{
    static void Main(string[] args)
    {
        //ServiceCollection  DI -> SOLI(D) -> Service Locator
        //ProfesorWeb Solid NINJECT -> SERVICE COLECTION / AUTOFAC(TORY)

        //IDBContext dl=new DataList();
        //IRequests r = new Requests(dl);
        //IOutput o = new Output(r);
        //IRun run = new Run(o);
        //run.Runner();
      
        
    }
}

